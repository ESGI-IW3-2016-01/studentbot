{% trans_default_domain 'FOSUserBundle' %}

<div class="white-section row">

    <div class="imac col-sm-12">
        <div class="white-section-text">
            <div class="imac-section-desc">
                <img src="/{{ user.getWebPath }}">
                <br>
                <br>
                <p>{{ "label_lastname" | trans({}, 'messages') }}: {{ user.lastname }}</p>
                <p>{{ "label_firstname" | trans({}, 'messages') }}: {{ user.firstname }}</p>

                {% if user.school is not empty %}
                    <p>{{ "label_school" | trans({}, 'messages') }}: {{ user.school }}</p>
                {% else %}
                    <p>{{ "label_school" | trans({}, 'messages') }}: {{ "not_specified" | trans({}, 'messages') }}</p>
                {% endif %}

                {% if user.studentGroup is not empty %}
                    <p>{{ "label_student_group" | trans({}, 'messages') }}: {{ user.studentGroup }}</p>
                {% else %}
                    <p>{{ "label_student_group" | trans({}, 'messages') }}: {{ "not_specified" | trans({}, 'messages') }}</p>
                {% endif %}
            </div>
            <a href="{{ path('fos_user_profile_edit') }}" class="btn btn-info"> <i class="fa fa-pencil-square-o" aria-hidden="true"></i> {{ "top_nav_edit_profil" | trans({}, 'messages') }}</a>
            
        </div>
    </div>

</div><!--white-section-text-section -->

{# <div class="footer">

    <div class="container">
        <div class="row">
            <div class="col-sm-2 col-sm-offset-4">
                <div class="social-links">
                    <a href="#"> <img class="social-link" src="/images/twitter.svg"/> </a>
                    <a href="#"> <img src="/images/facebook.svg"/> </a>
                </div>
                <!--social-links-->
            </div>
            <!--social-links-parent-->
        </div>
        <!--row-->

    </div>
    <!--container-->
</div>
 #}